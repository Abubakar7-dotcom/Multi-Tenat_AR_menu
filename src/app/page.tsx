export default function RootPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8 text-center">
      <p className="text-gray-500">
        This is the AR Menu platform root. Restaurant menus live at{" "}
        <code>/r/[slug]</code>.
      </p>
    </main>
  );
}
